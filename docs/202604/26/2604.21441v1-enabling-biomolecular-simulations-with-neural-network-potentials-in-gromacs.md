---
title: Enabling Biomolecular Simulations with Neural Network Potentials in GROMACS
title_zh: 在 GROMACS 中利用神经网络势实现生物分子模拟
authors: "Lukas Müllender, Berk Hess, Erik Lindahl"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21441v1"
tags: ["query:qmmm"]
score: 8.0
evidence: 混合机器学习/分子力学 (ML/MM) 模拟接口
tldr: 本研究在广泛使用的分子动力学软件GROMACS中实现了一个灵活的机器学习/分子力学（ML/MM）混合模拟接口。该接口支持PyTorch框架下的多种神经网络势能（NNP）架构，并能与GROMACS现有的增强采样和自由能计算工作流无缝集成。通过对肽链、溶剂化及蛋白质-配体系统的模拟验证，该工具为在生物分子模拟中应用机器学习势能提供了实用基础。
selection_source: fresh_fetch
motivation: 旨在解决现有分子动力学软件缺乏灵活且易用的神经网络势能（NNP）集成接口，以充分利用机器学习在模拟中的潜力。
method: 在GROMACS中开发了一个通用的ML/MM接口，支持PyTorch训练的NNP模型，并兼容多种模型架构及高级采样算法。
result: 成功演示了该接口在肽链构象采样、绝对溶剂化自由能计算及蛋白质-配体模拟中的应用，并提供了性能基准测试。
conclusion: 该接口为将机器学习势能引入生产级生物分子动力学模拟提供了坚实且实用的技术基础。
---

## 摘要
神经网络势 (NNPs) 正在迅速改变最先进的分子动力学 (MD) 模拟的格局。为了充分利用这一进展，研究社区需要与现有方法论紧密结合的灵活且易用的接口。为此，我们在此介绍一种在广泛使用的 MD 软件 GROMACS 中实现的混合机器学习/分子力学 (ML/MM) 模拟接口。该接口允许在 PyTorch 框架中训练的 NNP 在 MD 模拟过程中为选定的子集或整个分子系统提供能量和力。通过定义一组灵活的模型输入和输出，该接口与具体的 NNP 架构无关，能够兼容各种基于描述符和消息传递的模型。特别是，该设计将 NNP 推理无缝集成到广泛的 GROMACS 分子模拟生态系统中，使用户能够直接将 NNP 与现有的增强采样和自由能工作流相结合。我们通过几个具有代表性的应用展示了该接口的功能，包括多肽扭转自由能景观的增强采样、绝对溶剂化自由能计算以及蛋白质-配体模拟。我们还针对几种不同的 NNP 架构在水盒模型上运行了性能基准测试。我们的接口已在最近发布的 GROMACS 版本中提供，我们相信它将为在生物分子系统的生产级 MD 模拟中引入机器学习势提供实用的基础。

## Abstract
Neural network potentials (NNPs) are rapidly changing the landscape of state-of-the-art molecular dynamics (MD) simulations. To make full use of this development, the community needs flexible, easy-to-use interfaces firmly integrated with existing methodologies. To address this, we here present an interface for hybrid machine learning/molecular mechanics (ML/MM) simulations implemented in the widely used MD code GROMACS. The interface enables NNPs trained in the PyTorch framework to contribute energies and forces during MD simulations, either for selected subsets or entire molecular systems. By defining a flexible set of model inputs and outputs, the interface is agnostic to specific NNP architectures and can accommodate a wide range of descriptor-based and message-passing models. In particular, the design integrates NNP inference seamlessly into the extensive GROMACS molecular simulation ecosystem, providing users with the capability to straightforwardly combine NNPs with existing advanced sampling and free energy workflows. We demonstrate the capabilities of the interface using several representative applications, including enhanced sampling of peptide torsional free energy landscapes, absolute solvation free energy calculations, and protein--ligand simulations. We also run performance benchmarks on water boxes for several different NNP architectures. Our interface is available in recent GROMACS releases, and we believe it will provide a practical foundation for incorporating machine learning potentials into production MD simulations of biomolecular systems.

---

## 论文详细总结（自动生成）

这篇论文介绍并验证了在分子动力学（MD）模拟软件 GROMACS 中新集成的神经网络势（NNP）接口。以下是对该论文的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：传统的量子力学/分子力学（QM/MM）模拟虽然精确，但受限于量子化学计算的高昂成本，难以处理大规模生物分子系统的长时间采样。
*   **研究背景**：神经网络势（NNP）能以接近 QM 的精度和远低于 QM 的成本预测能量和力。然而，现有的 MD 软件缺乏一个灵活、易用且能与高级采样算法（如自由能计算、增强采样）深度集成的通用接口，限制了 NNP 在生物分子生产级模拟中的应用。

### 2. 方法论
*   **核心思想**：开发名为 `nnpot` 的接口，利用 PyTorch 的 C++ API（LibTorch）将 NNP 直接嵌入 GROMACS 的力计算循环中。
*   **关键技术细节**：
    *   **架构无关性**：接口不绑定特定模型，只要模型通过 TorchScript 序列化并符合定义的输入输出规范（如坐标、原子序数、晶胞信息），即可使用。
    *   **混合模拟（ML/MM）**：支持将系统划分为 ML 区域（由 NNP 处理）和 MM 区域（由经典力场处理）。
    *   **嵌入方案**：默认支持机械嵌入（Mechanical Embedding），并允许通过模型端实现电荷预测来支持静电嵌入（Electrostatic Embedding）。
    *   **共价键处理**：当 ML/MM 边界切断共价键时，接口会自动引入“链路原子”（Link Atoms），并根据链式法则重新分配受力。
    *   **集成性**：与 GROMACS 的 `MDModules` 框架结合，使其能直接调用 AWH（增强采样）和 alchemical（自由能）等现有功能。

### 3. 实验设计
论文通过四个代表性场景验证了接口的功能和性能：
*   **丙氨酸二肽（Alanine Dipeptide）**：在水中进行 AWH 增强采样，绘制拉氏图（Ramachandran plot），对比经典力场与 ANI2x 模型。
*   **FreeSolv 数据库**：选取 30 个小分子计算绝对溶剂化自由能（SFE），对比实验值、GAFF 力场值和 ANI2x 结果。
*   **蛋白质-配体系统**：模拟溶菌酶 L99A/M102Q 突变体与儿茶酚（Catechol）的结合，研究不同 ML 区域大小及嵌入方案（ME vs EE）对结合姿态稳定性的影响。
*   **性能基准测试**：在不同规模的水盒（3 到 30,000 原子）上测试了 ANI2x、MACE-OFF、AIMNet2 和 Nutmeg 等多种架构的推理速度。

### 4. 资源与算力
*   **硬件环境**：主要实验和基准测试在 **NVIDIA GeForce RTX 3070 GPU** 上运行。
*   **精度**：使用 32 位浮点精度（FP32）。
*   **算力细节**：论文给出了具体的吞吐量数据（如 ns/day）。例如，ANI2x 在小系统上可达约 50-80 ns/day，而 MACE 等复杂模型速度较慢。文中未提及具体的总训练时长，因为主要使用的是预训练模型。

### 5. 实验数量与充分性
*   **实验规模**：
    *   溶剂化自由能测试涵盖了 30 个分子（包括随机选择、误差最大和最小的样本），具有一定的统计代表性。
    *   蛋白质-配体模拟进行了 3 次独立重复实验以确保统计显著性。
    *   基准测试覆盖了从单分子到数万原子的广泛区间。
*   **充分性评价**：实验设计较为全面，不仅验证了精度，还验证了与复杂采样工作流的兼容性。对比了多种主流 NNP 架构，实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **功能集成**：`nnpot` 接口成功实现了 NNP 与 GROMACS 高级功能的无缝对接。
*   **精度提升**：在溶剂化自由能计算中，ANI2x 的平均绝对误差（1.20 kcal/mol）优于经典力场 GAFF（1.47 kcal/mol）。
*   **嵌入方案的重要性**：在蛋白质-配体模拟中发现，仅使用机械嵌入可能导致配体结合姿态不稳定，而引入静电嵌入（如 EMLE 模型）或扩大 ML 区域能显著改善模拟质量。
*   **性能瓶颈**：对于小型 ML 区域，性能受限于 LibTorch 的算子调度开销（API 调用延迟）；对于大型系统，计算量则成为主要限制。

### 7. 优点
*   **高度灵活性**：不限制 NNP 架构，用户可以轻松更换最新的模型（如从 ANI 换到 MACE）。
*   **易用性**：通过简单的 `.mdp` 文件配置即可激活，且自动处理链路原子，降低了使用门槛。
*   **生态兼容**：直接利用 GROMACS 强大的采样工具箱，无需用户自行编写复杂的耦合代码。

### 8. 不足与局限
*   **性能限制**：相比经过高度优化的经典力场，NNP 仍然慢 1-2 个数量级；且在 GPU 上处理极小系统时存在明显的调度延迟。
*   **自由能限制**：目前仅支持绝对自由能计算，尚不支持涉及原子拓扑变化的相对自由能计算（需要更复杂的双拓扑方案）。
*   **嵌入方案依赖**：机械嵌入在某些生物系统中表现不足，而更先进的静电嵌入方案仍依赖于特定模型的额外开发。
*   **版本依赖**：部分高级功能（如静电嵌入接口）仅在 GROMACS 2026 及更新版本中提供。

（完）
