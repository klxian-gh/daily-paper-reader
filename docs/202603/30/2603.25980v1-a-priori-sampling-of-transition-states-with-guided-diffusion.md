---
title: A Priori Sampling of Transition States with Guided Diffusion
title_zh: 利用引导扩散进行过渡态的先验采样
authors: "Hyukjun Lim, Soojung Yang, Lucas Pinède, Miguel Steiner, Yuanqi Du, Rafael Gómez-Bombarelli"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25980v1"
tags: ["query:qmmm"]
score: 8.0
evidence: 采样过渡态以研究动力学和机理
tldr: 过渡态是决定化学反应动力学的关键，但因路径复杂且依赖启发式假设，其搜索极具挑战。本文提出 ASTRA 框架，通过在已知亚稳态配置上训练扩散模型，将过渡态搜索转化为生成模型的推理问题。ASTRA 利用引导扩散定位亚稳态间的等密度面，并结合得分对齐上升（SAA）算法与物理力，实现对一阶过渡态的精确采样。该方法无需初始路径猜测，能有效发现多种反应路径，适用于生物分子和化学反应等复杂系统。
selection_source: fresh_fetch
motivation: 传统过渡态搜索方法过度依赖对反应路径或坐标的启发式假设，限制了其在缺乏先验知识或存在多重路径系统中的应用。
method: 提出 ASTRA 框架，利用在亚稳态上训练的扩散模型，通过引导推理和得分对齐上升（SAA）算法将分子配置驱动至过渡态。
result: 在二维势能面、生物分子构象变化和化学反应等基准测试中，ASTRA 展现了高精度的过渡态定位能力并发现了多种反应路径。
conclusion: ASTRA 将过渡态搜索转化为生成模型的推理缩放问题，为复杂分子系统的动力学研究和机制探索提供了强有力的新工具。
---

## 摘要
过渡态是势能面上的一阶鞍点，决定了化学反应和构象变化的动力学与机制。准确定位过渡态具有挑战性，因为过渡路径在拓扑上非常复杂，且可能通过多种不同路径的系综进行。现有方法通过引入关于路径或反应坐标的启发式假设来应对这些挑战，但这限制了它们在缺乏良好初始猜测或猜测排除了其他潜在相关路径时的适用性。我们提出通过引入 ASTRA（基于引导扩散的过渡态先验采样）来绕过这些启发式限制，该方法将过渡态搜索重新定义为生成模型的推理阶段缩放问题。ASTRA 在已知亚稳态的构型上训练一个基于分数的扩散模型。然后，ASTRA 通过条件分数的原则性组合，引导推理过程趋向于分隔亚稳态盆地的等密度面。随后，分数对齐上升（SAA）过程利用条件分数之间的差异来近似反应坐标，并将其与物理力结合，以驱动收敛至一阶过渡态。在从二维势能面到生物分子构象变化及化学反应的基准测试中，ASTRA 能够高精度地定位过渡态并发现多条反应路径，从而实现了对复杂分子系统的机制研究。

## Abstract
Transition states, the first-order saddle points on the potential energy surfaces, govern the kinetics and mechanisms of chemical reactions and conformational changes. Locating them is challenging because transition pathways are topologically complex and can proceed via an ensemble of diverse routes. Existing methods address these challenges by introducing heuristic assumptions about the pathway or reaction coordinates, which limits their applicability when a good initial guess is unavailable or when the guess precludes alternative, potentially relevant pathways. We propose to bypass such heuristic limitations by introducing ASTRA, A Priori Sampling of TRAnsition States with Guided Diffusion, which reframes the transition state search as an inference-time scaling problem for generative models. ASTRA trains a score-based diffusion model on configurations from known metastable states. Then, ASTRA guides inference toward the isodensity surface separating the basins of metastable states via a principled composition of conditional scores. A Score-Aligned Ascent (SAA) process then approximates a reaction coordinate from the difference between conditioned scores and combines it with physical forces to drive convergence onto first-order transition states. Validated on benchmarks from 2D potentials to biomolecular conformational changes and chemical reaction, ASTRA locates transition states with high precision and discovers multiple reaction pathways, enabling mechanistic studies of complex molecular systems.

---

## 论文详细总结（自动生成）

### 论文结构化总结：ASTRA——利用引导扩散进行过渡态的先验采样

#### 1. 核心问题与整体含义（研究动机和背景）
过渡态（Transition State, TS）是化学反应路径上的能量最高点（一阶鞍点），决定了反应速率和机制。然而，定位过渡态极具挑战：它们寿命极短，实验难以观测，且在分子动力学（MD）模拟中属于罕见事件。
*   **传统局限**：现有方法（如 NEB 或 Dimer 算法）高度依赖于初始路径猜测或预定义的集体变量（CVs）。如果初始猜测不准，往往无法收敛或遗漏重要的替代反应路径。
*   **研究目标**：提出一种名为 **ASTRA** 的框架，旨在无需预先了解反应路径、无需过渡态训练数据、不依赖启发式假设的情况下，仅通过反应物和产物的分布信息，自动采样并定位过渡态。

#### 2. 方法论：核心思想与关键技术
ASTRA 将过渡态搜索转化为生成模型在推理阶段的引导采样问题，主要包含三个阶段：
*   **条件扩散模型训练**：在反应物（状态 A）和产物（状态 B）的短 MD 轨迹上训练一个基于分数的扩散模型，利用无分类器引导（Classifier-Free Guidance, CFG）来学习两个亚稳态的概率分布。
*   **基于分数的插值（Score-Based Interpolation, SBI）**：在推理（生成）过程中，利用原则性的组合方式（如 SuperDiff 算法）合并两个状态的分数函数，引导采样过程停留在两个状态概率相等的“等密度面”上。这为过渡态提供了一个初步的区域猜测。
*   **分数对齐上升（Score-Aligned Ascent, SAA）**：这是本文的核心创新。
    *   **近似反应坐标**：利用两个条件分数之差（$s_B - s_A$）来近似反应坐标。
    *   **力引导优化**：在扩散过程的低噪声阶段暂停，结合物理力场（梯度）进行优化——在反应坐标方向上“爬坡”（能量上升），在正交方向上“降坡”（能量下降）。
    *   **流形修正**：优化后继续完成剩余的去噪步骤，将结构投影回合理的分子流形。

#### 3. 实验设计
研究者在多个维度上验证了 ASTRA 的有效性：
*   **数据集/场景**：
    1.  **2D 解析势能面**：双阱电势、Müller-Brown 电势、双路径电势（验证多路径发现能力）。
    2.  **生物分子构象**：丙氨酸二肽（Alanine Dipeptide）的异构化、Chignolin 蛋白的折叠。
    3.  **化学反应**：供体-受体 Stenhouse 加合物（DASA）的 4π-电环化反应（涉及共价键断裂与生成）。
*   **Benchmark 与对比方法**：
    *   **指标**：提交概率（Committor value $q \approx 0.5$）、L2 距离、Dimer 算法收敛率、RMSD、能量差。
    *   **对比方法**：线性插值（LI）、测地线插值（Geodesic Interpolation）、Nudged Elastic Band (NEB) 以及传统的 Dimer 优化。

#### 4. 资源与算力
*   **算力说明**：论文提到使用了 **MIT SuperCloud** 和林肯实验室超级计算中心的资源。
*   **模型架构**：使用了 **EquiformerV2**（一种等变 Transformer 架构）作为扩散模型的主干。
*   **具体细节**：文中给出了超参数（如 AdamW 优化器、学习率 $6 \times 10^{-4}$、500 个 epoch、DDPM 1000 步等），但**未明确说明具体的 GPU 型号、数量及总训练时长**。

#### 5. 实验数量与充分性
*   **实验组数**：涵盖了从简单 2D 模型到 42 个原子的复杂化学反应，实验设计具有阶梯性。
*   **消融实验**：非常充分。附录中详细讨论了引导缩放（GS）、暂停比例（PR）、优化步数（OS）和步长（SS）对结果的影响。
*   **客观性**：通过与公认的物理指标（如 Committor 分析）和标准优化工具（SCINE ReaDuct）对比，证明了生成结构的物理真实性。实验结果显示 ASTRA 能发现 NEB 容易遗漏的次要路径，体现了其探索的全面性。

#### 6. 主要结论与发现
*   **高精度定位**：ASTRA 生成的样本在 Committor 分析中高度集中在 0.5 附近，表明其精准捕捉到了过渡态区域。
*   **多路径发现**：在 Müller-Brown 和 Chignolin 实验中，ASTRA 无需额外输入即可同时发现多条竞争的反应路径。
*   **优秀的初始猜测**：作为单端优化（如 Dimer）的初始输入，ASTRA 样本的收敛速度和成功率远高于传统的线性插值。
*   **无需先验知识**：仅需亚稳态数据，即可推断出中间的过渡态，证明了扩散模型在插值超分布区域的潜力。

#### 7. 优点（亮点）
*   **端到端流程**：打破了传统方法对“初始路径猜测”的依赖，实现了过渡态的自动采样。
*   **物理与 AI 结合**：SAA 算法巧妙地将扩散模型的数学特性（分数差）与物理化学概念（反应坐标）结合。
*   **鲁棒性**：相比 NEB，SAA 在处理非物理初始结构时更具韧性，因为它可以通过去噪过程自我修正。

#### 8. 不足与局限
*   **计算开销**：虽然推理很快，但针对每个新系统都需要生成 MD 数据并训练特定的扩散模型，这在计算成本上可能高于简单的传统搜索。
*   **力场依赖**：SAA 阶段需要调用物理力场，其精度受限于底层力场（如 PM6 或经典力场）的准确性。
*   **采样效率**：在极高维度的生物大分子中，如何确保 SBI 能够覆盖所有相关的等密度面区域仍是一个挑战。
*   **数据需求**：虽然不需要过渡态数据，但仍需要反应物和产物盆地的充分采样数据。

（完）
