---
title: Discovering Reaction Mechanisms with Transition Path Sampling-Based Active Learning of Machine-Learned Potentials
title_zh: 基于过渡路径采样主动学习机器学习势能面以发现反应机制
authors: "Ashique Lal, Rik S. Breebaart, Peter G. Bolhuis, Evert Jan Meijer"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03737v1"
tags: ["query:qmmm"]
score: 8.0
evidence: 通过过渡路径采样发现反应机理
tldr: 本研究针对机器学习势函数（MLP）在过渡态区域可靠性不足的问题，提出了一种基于过渡路径采样（TPS）的主动学习框架。该框架利用TPS生成无偏反应轨迹，并通过委员会不确定性估计筛选关键构型进行DFT标注和模型迭代，无需预知反应机制即可精炼势能面。在铜电极二氧化碳还原反应的应用中，该方法显著提升了模型精度，揭示了多种质子化机制，为复杂电化学反应模拟提供了高效工具。
selection_source: fresh_fetch
motivation: 机器学习势函数在处理稀有事件的过渡态区域时往往精度不足，限制了其在复杂反应机制研究中的应用。
method: 提出一种基于过渡路径采样（TPS）的主动学习框架，通过不确定性估计筛选关键构型进行DFT标注并迭代训练。
result: 在铜电极表面二氧化碳还原反应的模拟中，该方法消除了物理伪影，达到了接近DFT的精度，并实现了稳定的长时反应路径采样。
conclusion: 该研究证明了TPS是一种高效且原则性的主动学习策略，能够有效发现和模拟电化学界面等复杂系统中的反应机制。
---

## 摘要
机器学习原子间势 (MLPs) 以较低的计算成本提供了接近密度泛函理论 (DFT) 的精度，但其可靠性取决于具有代表性的训练数据，且在控制稀有事件的过渡态区域往往表现不佳。我们引入了一个主动学习框架，其中过渡路径采样 (TPS) 作为一种有针对性的数据生成引擎，用于构建在能垒区域具有高精度的 MLPs。TPS 生成无偏反应轨迹系综，基于委员会的不确定性估计识别出特定构型，用于选择性的 DFT 标注和重训练。通过迭代这一循环，可以在无需先验机制知识的情况下，系统地优化动力学相关区域的势能面。应用于显式水环境下铜表面电化学 CO2 还原为 CO 的过程，该方法消除了早期模型中存在的非物理伪影，实现了接近 DFT 的能量和力精度，并能够对反应路径进行稳定的长时间采样。扩展的 TPS 模拟揭示了多种动力学上可行的质子化机制。这项工作确立了 TPS 作为一种高效且有原则的主动学习策略，适用于电化学界面的反应分子模拟。

## Abstract
Machine-learned interatomic potentials (MLPs) provide near density functional theory (DFT) accuracy at reduced computational cost, but their reliability depends on representative training data and often deteriorates in transition-state regions governing rare events. We introduce an active-learning framework in which Transition Path Sampling (TPS) serves as a targeted data-generation engine for constructing MLPs accurate in barrier regions. TPS generates ensembles of unbiased reactive trajectories, and a committee-based uncertainty estimate identifies configurations for selective DFT labeling and retraining. Iterating this cycle systematically refines the potential energy surface in dynamically relevant regions, without the need of prior knowledge of the mechanism. Applied to electrochemical CO$_2$ reduction to CO on copper in explicit water, the approach removes nonphysical artifacts present in early models, achieves near-DFT energy and force accuracy, and enables stable long-time sampling of reactive pathways. Extended TPS simulations reveal multiple dynamically accessible protonation mechanisms. This work establishes TPS as an efficient and principled active-learning strategy for reactive molecular simulations at electrochemical interfaces.

---

## 论文详细总结（自动生成）

这是一份关于论文《Discovering Reaction Mechanisms with Transition Path Sampling-Based Active Learning of Machine-Learned Potentials》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：电化学界面反应（如二氧化碳还原 CO2RR）涉及复杂的键重组、质子转移和溶剂重组。虽然从头算分子动力学（AIMD）精度高，但计算成本极高，难以模拟稀有事件；而机器学习势（MLP）虽快，但在缺乏训练数据的过渡态（能垒）区域往往精度骤降。
*   **核心问题**：如何系统地构建一个在反应过渡区域既准确又高效的机器学习势，且无需预先了解复杂的反应机制？
*   **整体含义**：本文提出将**过渡路径采样（TPS）**与**主动学习（Active Learning）**结合，利用 TPS 自动探索反应路径的特性，针对性地强化模型在能垒区域的描述能力。

### 2. 方法论
*   **核心思想**：将 TPS 作为主动学习的“数据生成引擎”。TPS 负责在当前 MLP 的驱动下生成无偏的反应轨迹，而主动学习通过不确定性估计筛选出模型“不擅长”的构型进行 DFT 标注。
*   **关键技术流程**：
    1.  **初始模型训练**：利用基础模型或受限 MD 生成少量初始数据，训练初步的 MLP（Gen 0）。
    2.  **TPS 采样**：在当前 MLP 势能面上运行 TPS（采用双向射击算法），生成连接反应物和产物的轨迹系综。
    3.  **不确定性评估（QbC）**：采用“委员会查询”（Query by Committee）策略，训练多个具有不同随机初始化的 MLP 模型。
    4.  **构型筛选**：计算轨迹上各构型的能量和力的方差，通过归一化处理得到不确定性评分 $S_i$，利用重要性采样选择高评分构型。
    5.  **迭代优化**：对选定构型进行 DFT 计算，更新训练集并重训练模型，循环往复直至模型在反应区域稳定。
*   **核心算法**：使用了 **MACE**（等变消息传递神经网络）架构作为 MLP 的基础。

### 3. 实验设计
*   **实验场景**：显式水环境下的铜（Cu）表面电化学 CO2 还原为 CO 的反应（$*CO_2 \rightarrow *COOH \rightarrow *CO + *OH$）。
*   **Benchmark（基准）**：使用 CP2K 软件在 PBE+D3 泛函级别下生成的 DFT 能量和力数据。
*   **对比对象**：对比了从 Gen 0 到 Gen 5 迭代过程中的模型表现，验证了模型在能量/力精度、物理合理性（消除非物理伪影）以及路径探索能力上的提升。

### 4. 资源与算力
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **计算规模**：
    *   DFT 计算：初始 2000 个构型，每代增加 1000 个，总计约 7000 个 DFT 标注点。
    *   TPS 采样：每代进行 2000 次 Monte Carlo 射击尝试，最终生产运行进行了 5000 次射击。
    *   模型参数：MACE 架构使用 32 通道，相关阶数为 3，截止半径 6.0 Å。

### 5. 实验数量与充分性
*   **实验组数**：进行了 6 代（Gen 0 至 Gen 5）的迭代训练。
*   **充分性评价**：
    *   **精度验证**：通过 RMSE 曲线展示了模型随迭代次数增加而收敛的过程。
    *   **物理验证**：展示了模型如何从最初产生非物理结构（如 OH 远离表面）进化到能够稳定描述复杂的界面氢键网络。
    *   **机制发现**：通过长达 5000 步的 TPS 采样，成功发现了两种不同的质子化路径，证明了实验的探索深度是充分的。

### 6. 主要结论与发现
*   **模型精度**：最终模型（Gen 5）的能量 RMSE 低于 1 meV/atom，力 RMSE 低于 0.05 eV/Å，达到了接近 DFT 的精度。
*   **机制发现**：
    *   **路径 1**：远离表面的氧原子被质子化形成 COOH，随后 OH 基团旋转靠近表面。
    *   **路径 2**：靠近表面的氧原子通过水分子网络的“质子跳跃”（Proton Hopping）被质子化，涉及更复杂的溶剂协同效应。
*   **稳定性**：该框架能有效消除早期模型中的非物理伪影，支持长时间、稳定的反应动力学采样。

### 7. 优点
*   **无需先验知识**：TPS 不需要预定义反应坐标（CV），能够自动发现复杂的、多维的反应路径。
*   **数据效率高**：通过主动学习精准定位能垒区域，避免了在平衡态区域进行无意义的大规模采样。
*   **无偏采样**：生成的轨迹是动力学无偏的，能够真实反映反应的动力学特征和机制多样性。

### 8. 不足与局限
*   **初始模型依赖**：如果初始模型（Gen 0）质量太差，无法生成任何成功的反应轨迹，TPS 引擎将无法启动。
*   **计算开销**：虽然比 AIMD 节省，但 TPS 涉及大量的轨迹积分，且每代仍需 1000 个 DFT 标注，对于超大规模系统仍有压力。
*   **应用限制**：目前仅验证了单步基元反应，对于涉及多个中间体、极长时标的多步连续反应，TPS 的采样效率可能会下降。

（完）
